<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Stokendra.ViewModels"
        xmlns:infra="using:Stokendra.Infrastructure"
        xmlns:models="using:Stokendra.Models"
        x:Class="Stokendra.Views.BulkEditServicesView"
        Title="{Binding Title}"
        Width="1050" Height="650"
        MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        CanResize="True"
        Background="#111522"
        x:CompileBindings="False">

    <Grid RowDefinitions="Auto,Auto,*,Auto">

        <!-- Header -->
        <Border Grid.Row="0" Background="#181D2A" Padding="24,16,24,14" BorderBrush="#1F2636" BorderThickness="0,0,0,1">
            <StackPanel Spacing="3">
                <TextBlock Text="{Binding Title}"
                           FontSize="20" FontWeight="Bold" Foreground="#E8EEFF"/>
                <TextBlock Text="Seçilen servis kayıtlarını toplu veya tekil olarak düzenleyin"
                           FontSize="12" Foreground="#64748B"/>
            </StackPanel>
        </Border>

        <!-- Bulk Apply Panel -->
        <Border Grid.Row="1" Background="#161B27" Margin="20,16,20,0"
                CornerRadius="10" Padding="16,14"
                BorderBrush="#1F2B3E" BorderThickness="1">
            <StackPanel Spacing="10">
                <TextBlock Text="{infra:L bulk_apply_header}" 
                           FontSize="12" FontWeight="Bold" Foreground="#38AEFF"/>
                
                <Grid ColumnDefinitions="*,*,Auto" RowDefinitions="Auto,Auto" RowSpacing="10" ColumnSpacing="16">
                    <!-- Date (Row 0, Col 0) -->
                    <Grid Grid.Row="0" Grid.Column="0" ColumnDefinitions="Auto,85,*" VerticalAlignment="Center">
                        <CheckBox Grid.Column="0" IsChecked="{Binding IsApplyDate, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Grid.Column="1" Text="{infra:L maintenance_date}" VerticalAlignment="Center" Foreground="#8A9BBF" FontSize="12"/>
                        <CalendarDatePicker Grid.Column="2" SelectedDate="{Binding ServiceDate, Mode=TwoWay}" IsEnabled="{Binding IsApplyDate}" Watermark="{infra:L date_filter}" HorizontalAlignment="Stretch" Height="28"/>
                    </Grid>

                    <!-- Company (Row 0, Col 1) -->
                    <Grid Grid.Row="0" Grid.Column="1" ColumnDefinitions="Auto,85,*" VerticalAlignment="Center">
                        <CheckBox Grid.Column="0" IsChecked="{Binding IsApplyCompany, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Grid.Column="1" Text="{infra:L company}" VerticalAlignment="Center" Foreground="#8A9BBF" FontSize="12"/>
                        <TextBox Grid.Column="2" Text="{Binding Company, Mode=TwoWay}" 
                                 IsEnabled="{Binding IsApplyCompany}"
                                 Height="28" Padding="6,4" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Problem (Row 1, Col 0) -->
                    <Grid Grid.Row="1" Grid.Column="0" ColumnDefinitions="Auto,85,*" VerticalAlignment="Center">
                        <CheckBox Grid.Column="0" IsChecked="{Binding IsApplyIssue, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Grid.Column="1" Text="{infra:L problem}" VerticalAlignment="Center" Foreground="#8A9BBF" FontSize="12"/>
                        <TextBox Grid.Column="2" Text="{Binding Issue, Mode=TwoWay}" 
                                 IsEnabled="{Binding IsApplyIssue}"
                                 Height="28" Padding="6,4" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Result (Row 1, Col 1) -->
                    <Grid Grid.Row="1" Grid.Column="1" ColumnDefinitions="Auto,85,*" VerticalAlignment="Center">
                        <CheckBox Grid.Column="0" IsChecked="{Binding IsApplyResult, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Grid.Column="1" Text="{infra:L result}" VerticalAlignment="Center" Foreground="#8A9BBF" FontSize="12"/>
                        <TextBox Grid.Column="2" Text="{Binding ResultText, Mode=TwoWay}" 
                                 IsEnabled="{Binding IsApplyResult}"
                                 Height="28" Padding="6,4" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Apply Button -->
                    <Button Grid.Row="0" Grid.RowSpan="2" Grid.Column="2" 
                            Content="{infra:L apply_btn}" 
                            Classes="Primary"
                            Command="{Binding ApplyBulkCommand}"
                            Width="90" Height="68"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Grid Container -->
        <Border Grid.Row="2" Background="#161B27" Margin="20,12,20,16"
                CornerRadius="10" BorderBrush="#1F2B3E" BorderThickness="1">
            <DataGrid ItemsSource="{Binding EditingRecords}"
                      SelectionMode="Single"
                      IsReadOnly="False"
                      CanUserResizeColumns="True"
                      Background="Transparent"
                      RowBackground="#161B27"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="{infra:L device_name}" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="models:ServiceRecord">
                                <TextBox Text="{Binding DeviceName, Mode=TwoWay}" Height="28" Padding="6,4" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="{infra:L serial_number}" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="models:ServiceRecord">
                                <TextBox Text="{Binding SerialNumber, Mode=TwoWay}" Height="28" Padding="6,4" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="{infra:L company}" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="models:ServiceRecord">
                                <TextBox Text="{Binding Company, Mode=TwoWay}" Height="28" Padding="6,4" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="{infra:L problem}" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="models:ServiceRecord">
                                <TextBox Text="{Binding Issue, Mode=TwoWay}" Height="28" Padding="6,4" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="{infra:L result}" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="models:ServiceRecord">
                                <TextBox Text="{Binding Result, Mode=TwoWay}" Height="28" Padding="6,4" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="{infra:L maintenance_date}" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate x:DataType="models:ServiceRecord">
                                <CalendarDatePicker SelectedDate="{Binding ServiceDate, Mode=TwoWay}" Height="28" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="3" Background="#181D2A" Padding="24,14,24,14" BorderBrush="#1F2636" BorderThickness="0,1,0,0">
            <Grid ColumnDefinitions="*,Auto">
                <TextBlock Grid.Column="0"
                           Text="{Binding ErrorMessage}"
                           Foreground="#FF453A"
                           FontSize="12"
                           FontWeight="Medium"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,0,16,0"
                           IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12">
                    <Button Content="{infra:L cancel}"
                            Classes="Secondary"
                            Command="{Binding CancelCommand}"
                            Width="100" MinHeight="36"/>
                    <Button Content="{infra:L save}"
                            Classes="Primary"
                            Command="{Binding SaveCommand}"
                            Width="100" MinHeight="36"/>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</Window>
